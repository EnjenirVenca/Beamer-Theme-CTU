%!TEX root = ../Main.tex

% Now both the page number on the slides and the section counter are reset to 1.
% In the appendix, please always use `\section*` (not `\section`), to avoid the display of outline pages.

\section*{Additional Illustrations}

\begin{frame}[fragile]
	\frametitle{Code block}

	\begin{lstlisting}[language=Python]
		# Python example code
		def greet(name):
			return f"Hello, {name}!"
		
		print(greet("World"))
	\end{lstlisting}
\end{frame}